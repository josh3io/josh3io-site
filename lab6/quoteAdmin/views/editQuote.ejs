<%- include("partials/header.ejs") %>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
      <div class="card shadow-sm p-4">

<h1 class="mb-4">Edit a Quote</h1>


    <form method="POST" action="/quote/edit">
      <input type="hidden" name="quoteId" value="<%= quote.quoteId%>">
      <div class="mb-3">
        <label class="form-label" for="quote">Quote:</label>
        <textarea class="form-control" name="quote" id="quote" rows="3"><%=quote.quote%></textarea>
      </div>
      <div class="mb-3">
        <label class="form-label" for="authorId">Author:</label>
        <select class="form-select" name="authorId" id="authorId">
          <% for (i=0; i < authors.length; i++) { %>
          <option
            <%= authors[i].authorId == quote.authorId ? 'selected' : '' %>
            value="<%=authors[i].authorId%>">
            <%=authors[i].firstName%> <%=authors[i].lastName%>
          </option>
          <% } %>
        </select>
      </div>
      <div class="mb-3">
        <label class="form-label" for="category">Select a Category:</label>
        <select class="form-select" name="category" id="category">
          <option value="">Select one or create new below</option>
          <% for (i=0; i < categories.length; i++) { %>
          <option 
            <%= categories[i].category == quote.category ? 'selected' : '' %>
          value="<%=categories[i].category%>"><%=categories[i].category%></option>
          <% } %>
        </select>
        <label class="form-label" for="new_category">or create a new category:</label>
        <div id="hideNewCategory" class="disabledDiv rounded d-none"></div>
        <input class="form-control" type="text" name="new_category" id="new_category">
      </div>
      <button class="btn btn-primary">Edit Quote</button>
    </form>

    <% if (typeof message != "undefined"){ %>
  		 <h3 class="mt-3 text-success"><%=message%></h3>
    <% } %>

      </div>
    </div>
  </div>
</div>
<script type="text/javascript" src="/js/quoteForm.js"></script>
<%- include("partials/footer.ejs") %>
</body>
</html>
